# nixpacks.toml - Railway multi-language build config
# This enables both Python AND Node.js so we can run Lighthouse CLI

providers = ["python", "node"]

[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["pip install -r requirements.txt", "npm install"]
