[pytest]
asyncio_mode = auto
markers =
    integration: hits real external network/APIs (websites, AI providers, Lighthouse). Skips itself when required API keys/tools aren't configured, so it's safe to leave enabled in CI.
